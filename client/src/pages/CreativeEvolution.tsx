import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, BarChart3, CheckCircle2, Film, Sparkles, ThumbsDown, ThumbsUp } from "lucide-react";
import React, { useMemo } from "react";
import { Link } from "wouter";

function sourceLabel(url: string) {
  try { return new URL(url).hostname.replace(/^www\./, ""); } catch { return "Public source"; }
}

function MetricBar({ label, value, max, accent = "bg-[#c7ff4b]" }: { label: string; value: number; max: number; accent?: string }) {
  const width = max ? Math.max(value ? 8 : 0, Math.round((value / max) * 100)) : 0;
  return <div><div className="flex items-center justify-between text-[10px] uppercase tracking-[.11em] text-white/38"><span>{label}</span><span className="font-mono text-white/62">{value}</span></div><div className="mt-1.5 h-1.5 overflow-hidden rounded-full bg-white/8"><div className={`h-full rounded-full ${accent}`} style={{ width: `${width}%` }} /></div></div>;
}

export default function CreativeEvolution() {
  const { loading, isAuthenticated } = useAuth({ redirectOnUnauthenticated: true });
  const comparisonQuery = trpc.mind.getRecommendationComparison.useQuery(undefined, { enabled: isAuthenticated });
  const comparison = comparisonQuery.data ?? [];
  const maxApplied = useMemo(() => Math.max(1, ...comparison.map(item => item.appliedPreferenceCount)), [comparison]);
  const maxClips = useMemo(() => Math.max(1, ...comparison.map(item => item.clipCount)), [comparison]);
  const maxFeedback = useMemo(() => Math.max(1, ...comparison.map(item => item.keptCount + item.correctedCount)), [comparison]);
  const first = comparison[0];
  const latest = comparison.at(-1);

  if (loading || !isAuthenticated) return <main className="workspace-bg grid min-h-screen place-items-center px-6 text-sm text-white/55">Opening your private Creative Evolution…</main>;

  return <main className="workspace-bg min-h-screen text-white">
    <header className="sticky top-0 z-30 border-b border-white/8 bg-[#08080b]/85 px-4 py-3 backdrop-blur-xl sm:px-6"><div className="mx-auto flex max-w-7xl items-center justify-between gap-3"><Link href="/" className="flex items-center gap-2.5"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-white text-black"><Sparkles size={15} strokeWidth={2.7} /></span><span className="font-display text-lg tracking-[-.04em]">SoulCut</span></Link><div className="flex items-center gap-2"><Link href="/dna" className="hidden rounded-full border border-white/10 px-3 py-2 text-xs text-white/55 transition hover:text-[#d8ff83] sm:block">Creative DNA</Link><Link href="/app" className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[.04] px-3 py-2 text-xs text-white/65 transition hover:border-[#c7ff4b]/45 hover:text-[#d8ff83]"><ArrowLeft size={14} /> Workspace</Link></div></div></header>
    <div className="mx-auto max-w-7xl px-4 py-7 sm:px-6 lg:py-10">
      <section className="relative overflow-hidden rounded-[2rem] border border-[#c7ff4b]/18 bg-[#10140f] p-6 sm:p-9"><div className="absolute -right-16 -top-20 h-64 w-64 rounded-full bg-[#c7ff4b]/10 blur-3xl" aria-hidden="true" /><div className="relative max-w-3xl"><p className="eyebrow text-[#d8ff83]">Private creative evolution</p><div className="mt-4 flex flex-wrap items-center gap-3"><h1 className="font-display text-5xl leading-[.86] tracking-[-.07em] sm:text-7xl">See what <span className="italic text-white/42">changed.</span></h1><span className="inline-flex items-center gap-1 rounded-full border border-[#c7ff4b]/20 bg-[#c7ff4b]/10 px-2 py-1 font-mono text-[9px] uppercase tracking-[.12em] text-[#d8ff83]"><BarChart3 size={10} /> Recorded history</span></div><p className="mt-5 max-w-2xl text-sm leading-relaxed text-white/55">This is an evidence view, not a performance score. It compares the Mind context applied at each analysis, stored clip opportunities, and feedback you actually recorded.</p></div></section>

      {comparisonQuery.isLoading ? <div className="mt-6 rounded-3xl border border-white/9 bg-white/[.025] p-7 text-sm text-white/45">Loading your recorded analysis history…</div> : comparison.length < 2 ? <section className="mt-6 rounded-3xl border border-dashed border-white/12 bg-white/[.025] p-8 text-center"><Film className="mx-auto text-[#d8ff83]" size={24} /><h2 className="mt-4 font-display text-3xl tracking-[-.055em]">Comparison starts with two completed videos.</h2><p className="mx-auto mt-3 max-w-md text-sm leading-relaxed text-white/45">SoulCut will show what changed once two completed analyses have persisted their Mind snapshots. Add a public source, optionally attach its creator-exported transcript, then compare the recorded context.</p><Link href="/app" className="mt-5 inline-flex rounded-full bg-[#e9ffe2] px-4 py-2.5 text-xs font-semibold text-[#111710]">Analyze a video</Link></section> : <><section className="mt-6 grid gap-4 md:grid-cols-3"><article className="rounded-2xl border border-white/9 bg-white/[.025] p-5"><p className="eyebrow text-[9px]">Applied preferences</p><p className="mt-3 font-display text-4xl tracking-[-.06em]">{first?.appliedPreferenceCount} <span className="text-white/35">→</span> {latest?.appliedPreferenceCount}</p><p className="mt-2 text-xs text-white/42">Analysis-time Creative DNA context, first to latest.</p></article><article className="rounded-2xl border border-white/9 bg-white/[.025] p-5"><p className="eyebrow text-[9px]">Opportunities recorded</p><p className="mt-3 font-display text-4xl tracking-[-.06em]">{comparison.reduce((sum, item) => sum + item.clipCount, 0)}</p><p className="mt-2 text-xs text-white/42">Timestamped clip suggestions across these completed briefs.</p></article><article className="rounded-2xl border border-white/9 bg-white/[.025] p-5"><p className="eyebrow text-[9px]">Feedback observed</p><p className="mt-3 font-display text-4xl tracking-[-.06em]">{comparison.reduce((sum, item) => sum + item.keptCount + item.correctedCount, 0)}</p><p className="mt-2 text-xs text-white/42">Kept or corrected recommendations, never inferred engagement.</p></article></section><section className="mt-6 rounded-3xl border border-white/9 bg-white/[.025] p-5 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow text-[9px]">Analysis timeline</p><h2 className="mt-2 font-display text-4xl tracking-[-.06em]">Recorded context, video by video.</h2></div><p className="max-w-xs text-xs leading-relaxed text-white/38">Bar lengths compare only the saved values in this view, not recommendation quality.</p></div><div className="mt-7 grid gap-3">{comparison.map((item, index) => <article key={item.jobId} className="grid gap-4 rounded-2xl border border-white/8 bg-black/20 p-4 lg:grid-cols-[72px_minmax(0,1fr)_minmax(300px,0.8fr)] lg:items-center"><div><p className="font-mono text-[10px] tracking-[.13em] text-[#d8ff83]">VIDEO {String(index + 1).padStart(2, "0")}</p><p className="mt-1 text-[10px] text-white/35">{new Date(item.createdAt).toLocaleDateString(undefined, { month: "short", day: "numeric" })}</p></div><div><p className="text-sm text-white/78">{sourceLabel(item.videoUrl)}</p><div className="mt-2 flex flex-wrap gap-1.5">{item.appliedPreferences.length ? item.appliedPreferences.slice(0, 4).map(preference => <span key={`${preference.category}-${preference.value}`} className="rounded-full border border-[#c7ff4b]/16 px-2 py-1 text-[10px] text-[#d8ff83]">{preference.value}</span>) : <span className="text-xs text-white/35">No recorded Mind context was available for this earlier job.</span>}</div>{item.transcriptFormat && <p className="mt-2 text-[10px] text-white/36">Creator-provided {item.transcriptFormat.toUpperCase()} transcript</p>}</div><div className="space-y-3"><MetricBar label="Applied Creative DNA" value={item.appliedPreferenceCount} max={maxApplied} /><MetricBar label="Clip opportunities" value={item.clipCount} max={maxClips} accent="bg-white/65" /><div className="grid grid-cols-2 gap-3"><MetricBar label="Kept" value={item.keptCount} max={maxFeedback} /><MetricBar label="Corrected" value={item.correctedCount} max={maxFeedback} accent="bg-red-300/75" /></div></div></article>)}</div></section></>}
    </div>
  </main>;
}
